// import getCurrentUser from "@/app/actions/getCurrentUser";

const User = () => {
    // const userInfo = await getCurrentUser();
    // console.log("user페이지 세션 정보" + userInfo)
    return (
        <div>유저페이지</div>
    )
}

export default User;